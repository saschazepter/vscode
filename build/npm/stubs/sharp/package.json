{
	"name": "sharp",
	"version": "0.34.5",
	"description": "Pure-JS stub of sharp. @huggingface/transformers imports sharp eagerly for its image backend, but VS Code only uses the audio (speech-to-text) pipeline, which never calls it. Stubbing avoids sharp's native (libvips) binary and its Electron build failures.",
	"main": "index.js",
	"license": "MIT"
}
