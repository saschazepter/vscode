version: 1
exchanges:
  - method: POST
    path: /responses
    dialect: responses
    request:
      model: gpt-5.3-codex
      system: ${system}
      messages:
        - role: user
          content: Call your listComments tool to list existing comments, then tell me exactly how many comments there are.
    response:
      content:
        - type: text
          text: I’m going to query the session comments now and then report the exact count.
        - type: tool_use
          id: toolcall_0
          name: listComments
          input: {}
      stopReason: tool_use
      usage:
        inputTokens: 9440
        outputTokens: 65
  - method: POST
    path: /responses
    dialect: responses
    request:
      model: gpt-5.3-codex
      system: ${system}
      messages:
        - role: user
          content: Call your listComments tool to list existing comments, then tell me exactly how many comments there are.
        - role: assistant
          content: I’m going to query the session comments now and then report the exact count.
        - role: assistant
          content:
            - type: tool_use
              name: listComments
              input: {}
        - role: user
          content:
            - type: tool_result
              tool_use_id: toolcall_0
              content: |-
                {
                  "comments": []
                }
    response:
      content: There are exactly **0 comments**.
      stopReason: end_turn
      usage:
        inputTokens: 9523
        outputTokens: 12
